A simple app using the ETSY API

The app fetches 100 shopIds and associated tagnames. The shopId, tagname, count are stored in a file and then the shopId : {top 5 tagnames based on count} are displayed on the terminal